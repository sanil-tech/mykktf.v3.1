{
  "name": "Student",
  "type": "object",
  "properties": {
    "student_id": {
      "type": "string",
      "description": "University student ID"
    },
    "full_name": {
      "type": "string"
    },
    "ic_passport": {
      "type": "string",
      "description": "IC or Passport number"
    },
    "gender": {
      "type": "string",
      "enum": [
        "Male",
        "Female"
      ]
    },
    "date_of_birth": {
      "type": "string",
      "format": "date"
    },
    "faculty": {
      "type": "string"
    },
    "programme": {
      "type": "string"
    },
    "year_of_study": {
      "type": "number",
      "enum": [
        1,
        2,
        3,
        4,
        5
      ]
    },
    "phone": {
      "type": "string"
    },
    "email": {
      "type": "string",
      "format": "email"
    },
    "block_name": {
      "type": "string",
      "description": "Assigned residential block"
    },
    "room_id": {
      "type": "string",
      "description": "Reference to Room entity ID"
    },
    "room_number": {
      "type": "string",
      "description": "Assigned room number"
    },
    "room_status": {
      "type": "string",
      "enum": [
        "Checked In",
        "Checked Out",
        "Pending Verification",
        "Pending Key"
      ],
      "default": "Checked Out"
    },
    "check_in_date": {
      "type": "string",
      "format": "date"
    },
    "parent_name": {
      "type": "string"
    },
    "parent_phone": {
      "type": "string"
    },
    "emergency_contact": {
      "type": "string"
    },
    "vehicle_reg": {
      "type": "string"
    },
    "profile_photo": {
      "type": "string",
      "format": "uri"
    },
    "status": {
      "type": "string",
      "enum": [
        "Active",
        "Inactive"
      ],
      "default": "Active"
    },
    "user_id": {
      "type": "string",
      "description": "Linked User account ID"
    },
    "qr_verified": {
      "type": "boolean",
      "default": false
    },
    "qr_verified_at": {
      "type": "string"
    },
    "resident_status": {
      "type": "string"
    }
  },
  "required": [
    "student_id",
    "full_name",
    "gender",
    "faculty",
    "phone",
    "email"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        },
        {
          "user_condition": {
            "role": "college_admin"
          }
        },
        {
          "user_condition": {
            "role": "staff"
          }
        },
        {
          "user_condition": {
            "role": "warden"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "super_admin"
          }
        }
      ]
    }
  }
}